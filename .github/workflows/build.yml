name: Build

on:
  push:
  pull_request:

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.detect.outputs.matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Detect submodules from settings.gradle.kts
        id: detect
        run: |
          JSON=$(grep '^include(' settings.gradle.kts \
            | sed 's/include("//;s/")//' \
            | jq -R . | jq -sc .)
          echo "matrix=$JSON" >> "$GITHUB_OUTPUT"
          echo "Submodules: $JSON"

  build:
    needs: detect
    runs-on: ubuntu-latest
    strategy:
      matrix:
        submodule: ${{ fromJson(needs.detect.outputs.matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'jetbrains'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Build with Gradle
        run: chmod u+x ./gradlew && ./gradlew ":${{ matrix.submodule }}:build"

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          path: ${{ matrix.submodule }}/build/libs/
          name: artifact-${{ matrix.submodule }}
